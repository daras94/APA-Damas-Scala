package damas.juego

object TestTablero {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(84); val res$0 = 
     Tablero.generarTablero(16, 16, 0, 5);;System.out.println("""res0: List[Int] = """ + $show(res$0))}
}
