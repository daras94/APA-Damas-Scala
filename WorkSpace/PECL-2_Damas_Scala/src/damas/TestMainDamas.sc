package damas

object TestMainDamas {
     /**
       *  Declaracion del MAIN.
       */
     def main(args: Array[ String ]): Unit = {
          //val tablero = Tab.generarTablero(16, 16, 0, 5);
          Gui.InitGUI();
     }                                            //> main: (args: Array[String])Unit
     
}